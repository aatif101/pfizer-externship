# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 migration cleanup | environment | Supported local runtime for development and verification | Use Python 3.11 project virtual environment for all project commands. | The global Python 3.14 environment has incompatible Pydantic/pydantic-settings packages, while the project venv on Python 3.11 supports editable install and test execution. | Yes | collaborative |
| D002 | M001 migration cleanup | security | Handling local provider/model settings | Remove settings.local.json from Git tracking and add it to .gitignore. | A token-like value had been tracked previously. Local provider keys and machine-specific model settings must remain outside version control. | No for secrets; Yes for local config file naming | collaborative |
| D003 | M001 migration cleanup | packaging | Python package build backend | Switch build backend from setuptools.backends.legacy:build to setuptools.build_meta. | The legacy backend failed editable installation in the Python 3.11 venv; the standard setuptools.build_meta backend supports editable installs successfully. | Yes | agent |
| D004 | M001 migration cleanup | workflow | GSD 1.0 project state migration strategy | Seed current .gsd PROJECT, REQUIREMENTS, and ROADMAP from the GSD 1.0 .planning artifacts. | The repo was created with GSD 1.0 .planning artifacts; current workflows need current-format root artifacts, requirement rows, and milestone/slice roadmap entries. | Yes | agent |
| D005 | M001 S02 planning | persistence | Document-level extraction summary table for Phase 2 compliance dashboard | Add a new compliance_records table while extending the existing extractions table for field-level evidence. | The existing extractions table is already the audit-friendly per-field evidence store, but S04 needs dashboard-ready one-row-per-document records for metadata, confidence, review state, and nullable risk fields without pivoting field rows on every render. | Yes | agent |
| D006 | M001 context interview | extraction-provider | Phase 2 live extraction provider stance | Use Gemini as the primary VLM extraction provider for M001. | Gemini 2.5 Flash is the stack recommendation for bulk extraction because it is vision-capable, supports structured outputs, and is much cheaper than Claude for folder-scale document processing; Claude can remain a later critic/fallback path. | Yes | human |
| D007 | M001 context interview | extraction-evidence | Evidence required before showing extracted fields as trustworthy | Require source page and verbatim span for every non-abstained field; bounding boxes remain optional in M001. | Page-plus-span evidence gives compliance reviewers an auditable basis without blocking Phase 2 on unproven bounding-box extraction for scanned/stamped PDFs. | Yes | human |
| D008 | M001 context interview | compliance-risk | Date basis for green/amber/red compliance age calculation | Use the most conservative date interpretation: expired documents are red; otherwise compute age from the oldest relevant lifecycle date, and flag missing or ambiguous dates for review. | A conservative policy is appropriate for pharmaceutical compliance and avoids under-flagging stale supplier documentation when multiple date fields disagree. | Yes | human |
| D009 | M001/S03 planning | extraction | Baseline extraction orchestration will use a provider protocol with Gemini behind an adapter and fake providers in default tests. | Define a small provider-neutral contract for extraction, keep Gemini API integration optional/configured at runtime, and make routine pytest coverage use fake/mocked providers only. | This lets S03 prove schema normalization, source grounding, risk computation, persistence, and failure handling deterministically without requiring live API credentials or network access while preserving a path to Gemini 2.5 Flash for real runs. | Yes | agent |
| D010 | M001/S03 planning | compliance | Baseline compliance risk uses a pure deterministic policy before persistence. | Compute document risk conservatively from expiry first, then oldest lifecycle date age with injectable current date: expired or >3 years red, 2-3 years amber, under 2 years green, and missing/ambiguous date evidence needs review. | Compliance workflows should fail safe, be deterministic under tests, and populate existing nullable dashboard fields in `compliance_records` without waiting for Streamlit UI work. | Yes | agent |
| D011 | M001/S03/T04 | extraction-cli | How the extraction CLI selects providers for live runs and offline tests | Default CLI provider construction is lazy Gemini, with a `build_provider()` seam that tests monkeypatch to inject fake providers; unsupported provider names and missing Gemini credentials return sanitized non-zero CLI errors. | This preserves the real live entrypoint behavior without requiring credentials at import time or during default pytest, while giving tests an executable CLI path that persists compliance rows through the production pipeline. | Yes | agent |
