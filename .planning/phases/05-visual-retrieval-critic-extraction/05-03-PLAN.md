---
phase: 05-visual-retrieval-critic-extraction
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/config.py
  - src/retrieval/models.py
  - src/retrieval/retriever.py
  - src/eval/retrieval_eval_runner.py
  - scripts/repair_gold_ex3_mojibake.py
  - tests/retrieval/visual/test_mode_select.py
  - tests/retrieval/visual/test_privacy_allowlist.py
  - tests/eval/test_gold_mojibake_repair.py
autonomous: true
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "A config setting retrieval_mode selects 'text-only' (default) vs 'visual-fused' so the same gold set can evaluate both modes"
    - "retrieve_evidence routes between text-only and the visual-fused seam based on retrieval_mode, with a stable signature so retrieval_eval_runner calls it unchanged"
    - "RetrievalScoreComponents.source accepts 'visual' and 'fused' without breaking existing text-tier construction"
    - "The 3 rq_ex3 gold queries containing U+FFFD are repaired to ÄKTA idempotently; no other gold rows change"
    - "Trace/eval allowlists exclude image bytes and full page text (any new visual keys are numeric/identifier only)"
  artifacts:
    - path: "src/config.py"
      provides: "retrieval_mode setting (text-only | visual-fused)"
      contains: "retrieval_mode"
    - path: "scripts/repair_gold_ex3_mojibake.py"
      provides: "Idempotent guarded gold mojibake repair"
      contains: "gold_retrieval_queries"
    - path: "src/retrieval/retriever.py"
      provides: "config-selectable fusion entry seam (text-only vs visual-fused)"
      contains: "retrieval_mode"
  key_links:
    - from: "src/eval/retrieval_eval_runner.py:153"
      to: "retrieve_evidence"
      via: "stable signature; retrieval_mode threaded via config/param"
      pattern: "retrieve_evidence"
    - from: "scripts/repair_gold_ex3_mojibake.py"
      to: "gold_retrieval_queries.query_text"
      via: "guarded UPDATE replacing U+FFFD with Ä"
      pattern: "UPDATE gold_retrieval_queries"
---

<objective>
Wire the visual tier into the existing system without changing any eval metric code: add a config-selectable `retrieval_mode` (text-only vs visual-fused), expose a fusion entry seam in `retrieve_evidence` with a stable signature, extend `RetrievalScoreComponents.source` to accept visual/fused, repair the `rq_ex3` gold mojibake idempotently, and prove the trace/eval privacy allowlists never leak image bytes or page text.

Purpose: VISUAL-01/VISUAL-02 must be benchmark-selectable (Phase 7) and must flow through the SAME `retrieve_evidence` → `RetrievalHit` → eval path. The Example-3 gold queries are corrupted (`�KTA`) and must be fixed. This plan is the integration seam; the GPU/quality numbers come from the notebook (Plan 04).
Output: `retrieval_mode` config + retriever routing, source-tag extension, idempotent gold repair script, and 3 offline test modules.
</objective>

<execution_context>
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-CONTEXT.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-02-PLAN.md
@.planning/quick/260611-ou3-widen-rag-evidence-from-truncated-index-/260611-ou3-SUMMARY.md

<interfaces>
<!-- Contracts the executor uses directly. From the codebase + Plans 01-02. -->

From src/config.py — Settings is a pydantic-settings BaseSettings (env_file=".env", extra="ignore"); ADD a new Field. get_settings() is lru_cached.
```python
class Settings(BaseSettings):
    db_path: str = Field(default="compliance.db", ...)
    ...   # ADD: retrieval_mode: str = Field(default="text-only", ...)
```

From src/retrieval/retriever.py:343-390 — retrieve_evidence signature (MUST stay call-compatible for retrieval_eval_runner.py:153 `retrieve_evidence(db_path, query_text, top_k=max_k)`):
```python
@observe(name="retrieval_evidence_gate")
def retrieve_evidence(db_path, question, *, top_k=5, candidate_limit=50,
    min_top_score=0.45, min_query_term_coverage=0.50, min_hit_count=1) -> RetrievalResult: ...
```
_RETRIEVAL_TRACE_ALLOWED_KEYS (retriever.py:31-41) = {boundary, run_id, evidence_reason, top_score, citation_count, is_strong, error_class} — the privacy gate. Add only numeric/identifier keys (e.g. "visual_hit_count", "retrieval_mode") if any; NEVER image bytes or page text.

From src/retrieval/models.py:103-112 — RetrievalScoreComponents.source is a free `str` (default "lexical"); text tier sets "fts"/"lexical". Visual tier sets "visual"/"fused" — the field already accepts any str, so document the allowed set in the docstring; no type change required.

From src/eval/retrieval_eval_runner.py:153-155 — the ONLY coupling; consumes (doc_id, page_num) only:
```python
retrieval_result = retrieve_evidence(db_path, query_text, top_k=max_k)
retrieved_pairs = [(hit.doc_id, int(hit.page_num)) for hit in retrieval_result.hits]
```
_EVALUATION_TRACE_ALLOWED_KEYS (retrieval_eval_runner.py:46-60) — extend with numeric keys only if needed.

From src/retrieval/visual/fusion.py (Plan 02) — rrf_fuse, to_retrieval_hits (the visual-fused seam target).

From scripts/relabel_gold_field_rules.py (analog) — PROJECT_ROOT bootstrap (:23-31), `from src.db.schema import _connect`, guarded UPDATE returning cursor.rowcount, prints only ids/counts, argparse --db-path. Header docstring style :1-14.

From compliance.db (VERIFIED this session) — the rows to repair (U+FFFD shown as �):
  rq_ex3_doc_type : "...Cytiva �KTA ready Gradient Flow Section."
  rq_ex3_mfg      : "...20210126 for �KTA ready Gradient Flow Section."
  rq_ex3_expiry   : "...20230126 for �KTA ready Gradient Flow Section."
  rq_ex3_vendor   : (NO mojibake — must remain untouched)
U+FFFD is the Python escape "�"; replacement target is "Ä" (U+00C4 = "Ä").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Idempotent gold mojibake repair script + test</name>
  <read_first>
    - scripts/repair_gold_ex3_mojibake.py (create)
    - scripts/relabel_gold_field_rules.py (analog — header docstring :1-14, PROJECT_ROOT bootstrap :23-31, guarded UPDATE + rowcount :42-70, argparse main)
    - tests/eval/test_gold_mojibake_repair.py (create)
    - src/db/schema.py (gold_retrieval_queries table :219-224)
  </read_first>
  <action>
    1. Create `scripts/repair_gold_ex3_mojibake.py` mirroring `relabel_gold_field_rules.py`: header docstring (idempotent, prints only query_ids/row-counts — never page text/secrets; compliance.db gitignored, only the script is committed), `PROJECT_ROOT = Path(__file__).resolve().parents[1]` + sys.path bootstrap, `from src.db.schema import _connect`, `DEFAULT_DB_PATH = str(PROJECT_ROOT / "compliance.db")`, argparse `--db-path`.
    2. Implement `repair_query(conn, query_id) -> int`: read the current query_text, compute `repaired = current.replace("�", "Ä")` (U+FFFD → Ä), and run a GUARDED update `UPDATE gold_retrieval_queries SET query_text = ? WHERE query_id = ? AND query_text = ?` with (repaired, query_id, current) so a second run changes 0 rows. Return cursor.rowcount. Only target the `rq_ex3_*` query_ids (loop the actual ids present: rq_ex3_doc_type, rq_ex3_mfg, rq_ex3_expiry, rq_ex3_vendor) — the vendor row has no U+FFFD so its repaired==current and the WHERE guard makes it a no-op naturally. Do NOT touch any non-rq_ex3 row.
    3. `main()` connects, applies repairs in a transaction, commits, prints `f"{query_id}: rows_changed={n}"` for each (no query text printed), and a total. Print a one-line safety reminder that compliance.db is never staged.
    4. Create `tests/eval/test_gold_mojibake_repair.py`: build a tmp SQLite db (tmp_db_path + init_db), INSERT a `rq_ex3_doc_type` row containing "�" plus an unrelated gold row, run the repair function, assert: (a) the rq_ex3 row now contains "Ä" ("ÄKTA") and NO "�"; (b) the unrelated row is byte-identical (untouched); (c) running the repair a SECOND time changes 0 rows (idempotent). This is a pure data-fix test (no GPU, no fabricated metric).
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/eval/test_gold_mojibake_repair.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep `UPDATE gold_retrieval_queries` and `�` and `Ä` in scripts/repair_gold_ex3_mojibake.py succeed.
    - grep `PROJECT_ROOT = Path(__file__).resolve().parents[1]` in the script succeeds (root bootstrap mirrored).
    - The script prints only query_ids + row counts (grep shows no `print(.*query_text` leaking text).
    - `venv\Scripts\python.exe -m pytest tests/eval/test_gold_mojibake_repair.py -x -q` passes (repair applies, unrelated row untouched, second run = 0 rows).
  </acceptance_criteria>
  <done>repair_gold_ex3_mojibake.py idempotently replaces U+FFFD with Ä in the rq_ex3 gold queries, leaves all other rows untouched, prints only ids/counts; test proves apply + isolation + idempotency.</done>
</task>

<task type="auto">
  <name>Task 2: retrieval_mode config + retriever fusion entry seam (text-only default)</name>
  <read_first>
    - src/config.py (modify — Settings class, add Field; get_settings lru_cache)
    - src/retrieval/models.py (modify — RetrievalScoreComponents docstring :103-112, document source ∈ {lexical, fts, visual, fused})
    - src/retrieval/retriever.py (modify — retrieve_evidence :343-390, _RETRIEVAL_TRACE_ALLOWED_KEYS :31-41, _safe_update_trace_metadata)
    - src/retrieval/visual/fusion.py (the visual-fused seam — Plan 02)
    - tests/retrieval/visual/test_mode_select.py (create), tests/retrieval/visual/test_privacy_allowlist.py (create)
    - tests/test_retrieval_eval_runner.py (analog — trace-capture monkeypatch + allowlist assertion :42-79)
  </read_first>
  <action>
    1. In `src/config.py` ADD `retrieval_mode: str = Field(default="text-only", description="Retrieval mode: 'text-only' (Phase 1 baseline) or 'visual-fused' (ColQwen2.5 visual tier fused via RRF)")`. Default MUST be "text-only" so existing behavior/tests are unchanged.
    2. In `src/retrieval/models.py` extend the `RetrievalScoreComponents` docstring to document `source ∈ {"lexical", "fts", "visual", "fused"}`. No type change (it is already `str`); existing construction stays valid.
    3. In `src/retrieval/retriever.py` add the config-selectable seam WITHOUT breaking the signature `retrieve_evidence(db_path, question, *, top_k=5, ...)` (retrieval_eval_runner.py:153 must keep working). Add an OPTIONAL keyword `retrieval_mode: str | None = None`; when None, resolve from `get_settings().retrieval_mode`. When mode == "text-only" (default): behavior is exactly as today (text gate). When mode == "visual-fused": call a new internal `_fused_evidence(...)` that obtains text-tier ranked hits as today, obtains visual ranked hits via a `visual_query_fn` seam (a callable defaulting to one that raises a clear `RuntimeError("visual-fused mode requires a built sdf_page_images index + GPU query — run via the Colab notebook")` if no visual backend is wired locally), then fuses with `rrf_fuse`/`to_retrieval_hits` (Plan 02) and returns the same `RetrievalResult`. The local default keeps text-only working with ZERO new heavy deps; visual-fused is exercised in the notebook (Plan 04). Do NOT relax indexer.py:192.
    4. Extend `_RETRIEVAL_TRACE_ALLOWED_KEYS` with numeric/identifier keys ONLY if you emit them: add `"retrieval_mode"` and `"visual_hit_count"` (both non-sensitive). NEVER add image bytes or page-text keys. Mirror `_safe_update_trace_metadata` filtering.
    5. Create `tests/retrieval/visual/test_mode_select.py`: assert default Settings.retrieval_mode == "text-only"; assert `retrieve_evidence(db_path, q)` with the default still returns text-tier results unchanged on a seeded tmp corpus (reuse the text-tier seed helpers from tests/test_retrieval_eval_runner.py); assert that requesting `retrieval_mode="visual-fused"` with no visual backend raises the clear RuntimeError (NOT a fabricated result — metric-integrity rule: no fake scores stand in for the model).
    6. Create `tests/retrieval/visual/test_privacy_allowlist.py` mirroring tests/test_retrieval_eval_runner.py:42-79: monkeypatch-capture `safe_update_current_trace` calls during a retrieve_evidence run, then assert captured metadata keys ⊆ `_RETRIEVAL_TRACE_ALLOWED_KEYS` AND that a forbidden-fragment list (`"image_blob"`, `"page_text"`, `"snippet"`, `"secret"`, b"\x89PNG") is absent from any serialized metadata value.
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_mode_select.py tests/retrieval/visual/test_privacy_allowlist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep `retrieval_mode` in src/config.py succeeds and the default is `"text-only"`.
    - grep `retrieval_mode` in src/retrieval/retriever.py succeeds; grep `def retrieve_evidence` still shows `db_path` + `question` + `top_k` so retrieval_eval_runner.py:153 stays call-compatible.
    - grep `visual-fused mode requires` (the clear RuntimeError) in retriever.py succeeds — no fabricated visual scores.
    - grep `image_blob`/`page_text` added to `_RETRIEVAL_TRACE_ALLOWED_KEYS` returns NOTHING.
    - `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_mode_select.py tests/retrieval/visual/test_privacy_allowlist.py -x -q` passes.
    - Existing retriever/eval tests still green: `venv\Scripts\python.exe -m pytest tests/test_retriever.py tests/test_retrieval_eval_runner.py -q`.
  </acceptance_criteria>
  <done>retrieval_mode config selects text-only (default, unchanged behavior) vs visual-fused; retrieve_evidence keeps a stable signature and routes to the RRF fusion seam; visual-fused without a backend raises a clear error (never a fake score); trace allowlist proven to exclude image bytes/page text; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieve_evidence → Langfuse trace | Only allowlisted numeric/identifier metadata may cross |
| gold repair script → compliance.db | Guarded UPDATE; only rq_ex3 rows; compliance.db never staged |
| visual-fused mode without a built index | Must fail loudly, never fabricate a score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure | retriever trace metadata | mitigate | _RETRIEVAL_TRACE_ALLOWED_KEYS extended with numeric keys only; test asserts captured keys ⊆ allowlist and forbidden fragments absent |
| T-05-12 | Tampering | gold mojibake repair | mitigate | Guarded UPDATE WHERE query_text=current; second run = 0 rows; test asserts non-rq_ex3 rows untouched |
| T-05-13 | Spoofing | fabricated visual results in CI | mitigate | visual-fused without a backend raises RuntimeError; no synthetic score path exists (metric-integrity rule) |
| T-05-14 | Information Disclosure | RetrievalHit.evidence_text on fused image-only pages | accept | Reuses Plan 02 empty-evidence behavior; bounded/in-memory only, never traced/persisted (quick-260611-ou3 contract) |
| T-05-15 | Tampering | retrieve_evidence signature drift | mitigate | retrieval_mode is an optional kwarg; eval runner call (db_path, query_text, top_k) stays valid; existing tests gate it |
</threat_model>

<verification>
- Full offline suite green: `venv\Scripts\python.exe -m pytest -q`
- Existing text-tier behavior unchanged with default config (retrieval_mode="text-only").
- No new module-top heavy imports introduced into retriever.py / config.py.
</verification>

<success_criteria>
- retrieval_mode config (text-only default | visual-fused) is selectable for the Phase 7 benchmark.
- retrieve_evidence routes to the RRF fusion seam under visual-fused while keeping a stable signature for the eval runner.
- RetrievalScoreComponents.source documents {lexical, fts, visual, fused}; existing construction unaffected.
- rq_ex3 gold mojibake repaired idempotently; no other gold rows changed.
- Trace/eval allowlists proven to exclude image bytes + page text; all offline tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-retrieval-critic-extraction/05-03-SUMMARY.md`
</output>
