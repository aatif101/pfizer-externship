---
phase: 05-visual-retrieval-critic-extraction
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/retrieval/visual/querier.py
  - src/retrieval/visual/fusion.py
  - src/retrieval/visual/run.py
  - src/retrieval/visual/repository.py
  - tests/retrieval/visual/test_query_payload.py
  - tests/retrieval/visual/test_fusion.py
  - tests/retrieval/visual/test_fused_hits.py
  - tests/retrieval/visual/test_run_versioning.py
autonomous: true
requirements: [VISUAL-02]

must_haves:
  truths:
    - "The two-stage query payload has exactly two pooled Prefetch entries (mean_pooling_columns + mean_pooling_rows) and reranks with using='original'"
    - "RRF fusion (k=60) over two ranked lists keyed on (doc_id, page_num) produces the known correct fused order"
    - "Fused hits map to RetrievalHit with 0-indexed page_num, display_page_num = page_num+1, and score_components.source in {visual, fts, fused}"
    - "Image-only fused pages (no text match) carry empty snippet/evidence_text (consistent with the _bounded_evidence_text empty-page fallback)"
    - "A visual_index_runs row persists with a deterministic visual-built-{hash} run_id carrying counts/model_version/collection_name only"
  artifacts:
    - path: "src/retrieval/visual/fusion.py"
      provides: "Pure RRF fusion → RetrievalHit mapping"
      contains: "def rrf_fuse"
    - path: "src/retrieval/visual/querier.py"
      provides: "Pure two-stage query payload builder (execution deferred to GPU/notebook)"
      contains: "def build_query_payload"
    - path: "src/retrieval/visual/run.py"
      provides: "Versioned visual index run builder mirroring retrieval_index_runs"
      contains: "deterministic_visual_run_id"
    - path: "src/retrieval/visual/repository.py"
      provides: "save_visual_index_run + get_latest_visual_index_run_id persistence"
      contains: "def save_visual_index_run"
  key_links:
    - from: "src/retrieval/visual/fusion.py rrf_fuse"
      to: "RetrievalHit (doc_id, page_num)"
      via: "RRF k=60 over (doc_id, page_num) → RetrievalHit"
      pattern: "RetrievalHit"
    - from: "src/retrieval/visual/querier.py build_query_payload"
      to: "Qdrant query_points prefetch"
      via: "2 pooled Prefetch + using='original'"
      pattern: "using.*original"
    - from: "src/retrieval/visual/repository.py save_visual_index_run"
      to: "visual_index_runs table"
      via: "INSERT ... ON CONFLICT(run_id) DO UPDATE"
      pattern: "visual_index_runs"
---

<objective>
Build the offline-testable retrieval logic for the visual tier: the two-stage Qdrant query payload builder, the RRF (k=60) fusion that maps visual+text candidates into the existing `RetrievalHit` DTO, and the versioned `visual_index_runs` persistence (mirroring `retrieval_index_runs`). All pure/deterministic — the GPU forward pass and live Qdrant execution are deferred to the notebook (Plan 04).

Purpose: VISUAL-02's two-stage strategy and fusion seam must produce the SAME `RetrievalHit` keyed on `(doc_id, page_num)` that `retrieve_evidence()` / the eval harness already consume — so eval metric code needs ZERO change. RRF math, payload shape, and run versioning are deterministic and unit-testable without a GPU, honoring the metric-integrity rule.
Output: `querier.py`, `fusion.py`, `run.py`, `repository.py` in `src/retrieval/visual/`, plus 4 offline test modules.
</objective>

<execution_context>
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-CONTEXT.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-01-PLAN.md

<interfaces>
<!-- Contracts the executor uses directly. From the codebase + Plan 01. -->

From src/retrieval/models.py (the DTOs fusion MUST emit unchanged):
```python
@dataclass(frozen=True)
class RetrievalScoreComponents:
    fts_score: float = 0.0
    lexical_score: float = 0.0
    token_coverage: float = 0.0
    phrase_bonus: float = 0.0
    proximity_bonus: float = 0.0
    source: str = "lexical"      # ← visual tier sets "visual" | "fused" | "fts"
@dataclass(frozen=True)
class RetrievalHit:
    doc_id: str
    filename: str
    page_num: int                # 0-indexed
    display_page_num: int        # page_num + 1
    score: float
    score_components: RetrievalScoreComponents
    snippet: str
    evidence_text: str = ""      # empty for image-only pages
```

From src/retrieval/retriever.py:307-336 — _score_candidate, the canonical RetrievalHit construction to mirror (source tag, round(score,4), 0/1-index, evidence_text fallback). Image-only pages with no text keep snippet/evidence_text empty (retriever.py:438-457 _bounded_evidence_text fallback established in quick-260611-ou3).

From RESEARCH Pattern 5 — RRF (k=60):
```python
def rrf_fuse(visual_ranked, text_ranked, *, k: int = 60):
    scores: dict[tuple[str, int], float] = {}
    for ranked in (visual_ranked, text_ranked):
        for rank, (doc_id, page_num) in enumerate(ranked):   # rank 0-based
            scores[(doc_id, page_num)] = scores.get((doc_id, page_num), 0.0) + 1.0/(k + rank + 1)
    return sorted(scores.items(), key=lambda kv: -kv[1])
```

From RESEARCH Pattern 4 — two-stage query (lazy-import qdrant_client.models INSIDE the function):
```python
client.query_points(
    collection_name="sdf_page_images_v1",
    query=query_embedding,
    prefetch=[
        models.Prefetch(query=query_embedding, limit=prefetch_limit, using="mean_pooling_columns"),
        models.Prefetch(query=query_embedding, limit=prefetch_limit, using="mean_pooling_rows"),
    ],
    limit=search_limit, with_payload=True, with_vector=False,
    using="original",   # full-multivector MAX_SIM rerank
)
```

From Plan 01 (src/retrieval/visual/models.py + collection.py):
```python
class VisualIndexRun: run_id, status, built_at, source_document_count,
    source_page_count, indexed_page_count, model_version="", collection_name="",
    content_hash=None, previous_content_hash=None
def deterministic_visual_run_id(content_hash: str) -> str   # "visual-built-{hash[:16]}"
def collection_name(version: int = 1) -> str                # "sdf_page_images_v{version}"
```

From src/retrieval/repository.py:87-130 — save_index_run uses INSERT ... ON CONFLICT(run_id) DO UPDATE ...; DB access via `from src.db.schema import _connect, init_db`; parameterized SQL only.
From src/eval/repository.py:624 — get_latest_retrieval_index_run_id (ORDER BY built_at DESC, run_id DESC LIMIT 1) — mirror for get_latest_visual_index_run_id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure two-stage query payload builder (querier.py)</name>
  <read_first>
    - src/retrieval/visual/querier.py (create)
    - src/retrieval/retriever.py (analog — _fts_candidates :247-279 candidate-fetch-then-map shape)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md (Pattern 4 two-stage query :278-293)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md (querier.py assignment :109-128 — split pure build_query_payload vs execution)
  </read_first>
  <behavior>
    - Test: `build_query_payload(query_embedding, *, prefetch_limit, search_limit, version=1)` returns a dict/namedtuple whose `prefetch` list has EXACTLY two entries, using "mean_pooling_columns" and "mean_pooling_rows" respectively, each with limit==prefetch_limit; the top-level rerank uses `using="original"`, `limit==search_limit`, `with_payload=True`, `with_vector=False`, and `collection_name == "sdf_page_images_v1"`.
    - Test: map_response_to_candidates(points) reads payload {doc_id, page_num} and the score, returning candidates carrying 0-indexed page_num (int) and display_page_num = page_num+1; a point missing doc_id/page_num is skipped (not crashed).
  </behavior>
  <action>
    1. Create `src/retrieval/visual/querier.py` with the provider-free/privacy module docstring. Split per PATTERNS.md: a PURE `build_query_payload(...)` (testable offline) and a thin `execute_query(client, ...)` that calls `client.query_points(**payload)` (notebook/GPU only — NOT asserted offline).
    2. Implement `build_query_payload(query_embedding, *, prefetch_limit=200, search_limit=20, version=1)` — lazy `from qdrant_client import models` INSIDE the function. Build `prefetch=[models.Prefetch(query=query_embedding, limit=prefetch_limit, using="mean_pooling_columns"), models.Prefetch(query=query_embedding, limit=prefetch_limit, using="mean_pooling_rows")]` and return the full kwargs dict for `query_points`: `collection_name=collection_name(version)`, `query=query_embedding`, `prefetch=...`, `limit=search_limit`, `with_payload=True`, `with_vector=False`, `using="original"`. Import `collection_name` from `src.retrieval.visual.collection`.
    3. Implement `map_response_to_candidates(points) -> list[VisualCandidate]` where `VisualCandidate` is a frozen dataclass `(doc_id: str, page_num: int, display_page_num: int, score: float)`. For each point read `payload["doc_id"]`, `int(payload["page_num"])` (0-indexed), `score`; set `display_page_num = page_num + 1`; skip points missing doc_id/page_num. NEVER read image bytes/page text from the payload (there are none — Plan 01 payload is identifiers only).
    4. Create `tests/retrieval/visual/test_query_payload.py` (`pytest.importorskip("qdrant_client")`): assert the 2-pooled-prefetch + `using="original"` shape, the prefetch `using` names, and the limit/with_payload/with_vector flags. Add an offline `map_response_to_candidates` test driven by fake point objects (simple stand-ins exposing `.payload`/`.score`) — this asserts identity mapping (0-indexed page_num, display+1), NOT a retrieval-quality number.
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_query_payload.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep `def build_query_payload` and `def map_response_to_candidates` in querier.py succeed.
    - grep `using="original"` and both `using="mean_pooling_columns"` and `using="mean_pooling_rows"` in querier.py succeed.
    - grep `from qdrant_client import models` in querier.py is inside a function body only; `venv\Scripts\python.exe -c "import src.retrieval.visual.querier"` succeeds with no qdrant-client installed.
    - grep `int(payload["page_num"])` (or equivalent int cast) in querier.py succeeds; grep `image_blob`/`page_text` return NOTHING.
    - `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_query_payload.py -x -q` passes.
  </acceptance_criteria>
  <done>build_query_payload yields the canonical two-stage prefetch (2 pooled prefetches + using='original'); map_response_to_candidates produces 0-indexed identity candidates; module imports offline-safe; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure RRF fusion → RetrievalHit mapping (fusion.py)</name>
  <read_first>
    - src/retrieval/visual/fusion.py (create)
    - src/retrieval/retriever.py (analog — _score_candidate :307-336 RetrievalHit construction; _bounded_evidence_text :438-457 empty-page fallback)
    - src/retrieval/models.py (RetrievalHit :115-132, RetrievalScoreComponents :103-112 — source field)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md (Pattern 5 RRF :295-306)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md (fusion.py assignment :132-153)
  </read_first>
  <behavior>
    - Test: `rrf_fuse([(d,p)...visual], [(d,p)...text], k=60)` with two known ranked lists returns the exact known fused order; a page in BOTH tiers ranks above a page in only one (score = sum of 1/(60+rank+1) across tiers).
    - Test: a page present only in the visual list (image-only, e.g. ("5543408c4dacc48b", 2)) still appears in the fused result (the Example-3 proof shape — image-only pages become reachable).
    - Test: `to_retrieval_hits(fused, lookup)` maps each fused (doc_id, page_num) to a RetrievalHit with page_num 0-indexed, display_page_num == page_num+1, score == round(rrf_score,4), score_components.source == "fused" (or "visual" when text-absent); for an image-only page with no text in `lookup`, snippet=="" and evidence_text=="".
  </behavior>
  <action>
    1. Create `src/retrieval/visual/fusion.py` with the provider-free/privacy module docstring (imports only stdlib + src.retrieval.models — no heavy deps, fully offline).
    2. Implement `rrf_fuse(visual_ranked, text_ranked, *, k: int = 60)` EXACTLY per RESEARCH Pattern 5: dict keyed on `(doc_id, page_num)`, `score += 1.0/(k + rank + 1)` for each tier (rank 0-based), return list sorted by descending fused score (stable tie-break on (doc_id, page_num) for determinism).
    3. Implement `to_retrieval_hits(fused, lookup, *, visual_only_ids=frozenset())` → `tuple[RetrievalHit, ...]`. `lookup` maps (doc_id, page_num) → a small record with `filename`, optional `page_text`/`snippet` from the TEXT tier. For each fused entry build a `RetrievalScoreComponents(source=...)` where source = "visual" if the page came only from the visual list, else "fused"; build `RetrievalHit(doc_id, filename, page_num (0-indexed), display_page_num=page_num+1, score=round(rrf_score,4), score_components, snippet, evidence_text)`. For image-only pages (no text in lookup) set `snippet=""` and `evidence_text=""` (mirror retriever.py:438-457 empty-page fallback — do NOT fabricate text). NEVER place image bytes anywhere.
    4. Create `tests/retrieval/visual/test_fusion.py` (known ranked lists → known fused order; image-only page survives fusion) and `tests/retrieval/visual/test_fused_hits.py` (RetrievalHit mapping: 0/1-index, source tag, empty evidence_text for image-only). These assert deterministic RRF MATH and DTO mapping — NOT a model-derived recall/ndcg number (metric-integrity rule).
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_fusion.py tests/retrieval/visual/test_fused_hits.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep `def rrf_fuse` and `def to_retrieval_hits` in fusion.py succeed; grep `k: int = 60` (default k=60) succeeds.
    - grep `1.0 / (k + rank + 1)` (or arithmetically equivalent) in fusion.py succeeds.
    - grep `source="fused"` and `source="visual"` in fusion.py succeed.
    - `venv\Scripts\python.exe -c "import src.retrieval.visual.fusion"` succeeds (no heavy deps).
    - test_fusion.py asserts the image-only page ("5543408c4dacc48b", 2) appears in fused output; test_fused_hits.py asserts empty evidence_text for an image-only page.
    - `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_fusion.py tests/retrieval/visual/test_fused_hits.py -x -q` passes.
  </acceptance_criteria>
  <done>rrf_fuse (k=60) deterministically fuses visual+text ranked lists keyed on (doc_id,page_num); image-only pages survive fusion; to_retrieval_hits emits canonical RetrievalHit with correct source tag, 0/1-index, and empty evidence_text for image-only pages; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Versioned visual index-run builder + persistence (run.py, repository.py)</name>
  <read_first>
    - src/retrieval/visual/run.py (create), src/retrieval/visual/repository.py (create)
    - src/retrieval/indexer.py (analog — build_retrieval_index :69-121, _deterministic_run_id :249, compute_indexable_corpus_fingerprint :214)
    - src/retrieval/repository.py (analog — save_index_run :87-130 INSERT ON CONFLICT; load_latest_index_run :169-186)
    - src/eval/repository.py (analog — get_latest_retrieval_index_run_id :624)
    - src/retrieval/visual/models.py (VisualIndexRun, deterministic_visual_run_id — from Plan 01)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md (run.py assignment :157-168 — separate tier, indexes ALL pages incl. image-only; does NOT relax indexer.py:192)
  </read_first>
  <behavior>
    - Test: `save_visual_index_run(db_path, run)` inserts a row into visual_index_runs; calling it again with the same run_id updates (ON CONFLICT) rather than duplicating (one row).
    - Test: the persisted row stores run_id/status/counts/model_version/collection_name ONLY — querying the row yields no image bytes / page text column.
    - Test: `get_latest_visual_index_run_id(db_path)` returns the most recent run_id, None when none exist.
    - Test: `plan_visual_run(pages_meta, *, model_version, version)` produces a VisualIndexRun whose run_id == deterministic_visual_run_id(content_hash) and whose source counts equal the input corpus (covering ALL pages incl. image-only — it does NOT apply the text indexer's empty-text exclusion).
  </behavior>
  <action>
    1. Create `src/retrieval/visual/repository.py` (provider-free DB docstring; `from src.db.schema import _connect, init_db`). Implement `save_visual_index_run(db_path, run: VisualIndexRun)` mirroring repository.save_index_run :87-130 — `INSERT INTO visual_index_runs (run_id, status, built_at, source_document_count, source_page_count, indexed_page_count, model_version, collection_name, content_hash, previous_content_hash, is_stale, stale_reason, error_reason) VALUES (...) ON CONFLICT(run_id) DO UPDATE SET ...`. Parameterized SQL only; built_at COALESCE to strftime now. Implement `get_latest_visual_index_run_id(db_path) -> str | None` mirroring eval/repository.py:624 (SELECT run_id FROM visual_index_runs ORDER BY built_at DESC, run_id DESC LIMIT 1).
    2. Create `src/retrieval/visual/run.py` (provider-free docstring). Implement `plan_visual_run(pages_meta, *, model_version, version=1, previous_content_hash=None) -> VisualIndexRun`. `pages_meta` is a sequence of identifier-only page descriptors `(doc_id, page_num, filename)` for ALL corpus pages INCLUDING image-only ones (this is a SEPARATE tier — explicitly does NOT reuse indexer.load_indexable_pages with its :192 empty-text exclusion; PATTERNS.md :168, RESEARCH Anti-Pattern). Compute a deterministic content hash over the sorted (doc_id, page_num, filename) tuples (mirror compute_indexable_corpus_fingerprint shape, but over identifiers — NO page text needed since visual indexes images). Set `run_id = deterministic_visual_run_id(content_hash)`, status "built", counts = distinct doc count / page count, `collection_name = collection_name(version)`, `model_version`. Provide a thin `build_visual_index_run(db_path, pages_meta, *, model_version, version=1)` that plans + persists via save_visual_index_run and returns the run (the GPU embed/upsert step is the notebook's job — Plan 04; this function records run metadata only).
    3. Create `tests/retrieval/visual/test_run_versioning.py` (uses tmp_db_path + init_db): save→re-save idempotency (one row), get_latest returns the run_id, persisted-row-has-no-raw-content assertion, and plan_visual_run determinism (same pages → same run_id; counts cover image-only pages). All deterministic, no GPU, no fabricated metric.
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_run_versioning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep `def save_visual_index_run`, `def get_latest_visual_index_run_id` in repository.py succeed; grep `ON CONFLICT(run_id) DO UPDATE` succeeds.
    - grep `def plan_visual_run`, `def build_visual_index_run` in run.py succeed; grep `deterministic_visual_run_id` in run.py succeeds.
    - grep `load_indexable_pages` in run.py returns NOTHING (visual tier does NOT reuse the empty-text-excluding text loader).
    - grep `image_blob`/`page_text` in repository.py and run.py return NOTHING.
    - `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_run_versioning.py -x -q` passes (idempotent save = 1 row; get_latest works; run_id deterministic; image-only pages counted).
  </acceptance_criteria>
  <done>save_visual_index_run persists idempotently (ON CONFLICT); get_latest_visual_index_run_id returns the newest run; plan_visual_run yields a deterministic visual-built-{hash} run over ALL pages incl. image-only (not reusing the text loader); rows carry counts/run_id/model_version/collection_name only; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant response payload → VisualCandidate | Only identifiers (doc_id/page_num) + score may cross; no raw content |
| Fused RetrievalHit → eval/RAG consumers | Hits must carry correct 0-indexed page identity; evidence_text bounded/empty for image-only |
| visual_index_runs persistence | Run rows must hold metadata only (counts/run_id/model_version/collection_name) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Spoofing | fused page identity (0/1-index) | mitigate | rrf_fuse + to_retrieval_hits key on 0-indexed (doc_id,page_num); test asserts display_page_num=page_num+1 (prevents silent recall@k=0, Pitfall 6) |
| T-05-07 | Information Disclosure | visual_index_runs row | mitigate | Persistence stores counts/run_id/model_version/collection_name only; grep proves no image_blob/page_text; test asserts row has no raw-content |
| T-05-08 | Information Disclosure | fused evidence_text for image-only pages | mitigate | Image-only pages keep evidence_text="" (no fabricated grounding); mirrors quick-260611-ou3 bounded-evidence contract |
| T-05-09 | Tampering | RRF fusion non-determinism | mitigate | Stable tie-break on (doc_id,page_num); test asserts exact known fused order |
| T-05-10 | Repudiation | visual run provenance | mitigate | Deterministic visual-built-{hash} run_id makes the build reproducible/auditable (mirrors retrieval_index_runs) |
</threat_model>

<verification>
- Full offline suite green: `venv\Scripts\python.exe -m pytest -q`
- All new visual modules import with NO heavy deps: `venv\Scripts\python.exe -c "import src.retrieval.visual.querier, src.retrieval.visual.fusion, src.retrieval.visual.run, src.retrieval.visual.repository"`
- No fabricated retrieval-quality metric anywhere in the new tests (RRF math + DTO mapping + payload shape + run versioning only).
</verification>

<success_criteria>
- build_query_payload yields the canonical two-stage prefetch (2 pooled prefetches + using='original').
- rrf_fuse (k=60) deterministically fuses visual+text ranked lists keyed on (doc_id,page_num); image-only pages survive.
- to_retrieval_hits emits canonical RetrievalHit (0/1-index, source tag, empty evidence_text for image-only pages).
- visual_index_runs persists idempotently; get_latest returns newest; plan_visual_run is deterministic over ALL pages incl. image-only.
- All new offline tests green; modules offline-importable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-retrieval-critic-extraction/05-02-SUMMARY.md`
</output>
