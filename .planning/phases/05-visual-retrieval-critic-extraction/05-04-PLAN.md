---
phase: 05-visual-retrieval-critic-extraction
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/retrieval/visual/embedder.py
  - notebooks/visual_retrieval_colab.ipynb
  - tests/retrieval/visual/test_embedder_offline.py
  - tests/retrieval/visual/test_notebook_structure.py
autonomous: false
requirements: [VISUAL-01, VISUAL-02]
user_setup:
  - service: "Google Colab Pro (L4 GPU)"
    why: "ColQwen2.5-v0.2 (3B) embedding needs a >=16 GB GPU in bf16; the local 8 GB RTX 5070 (Blackwell sm_120) is incompatible with the pinned torch<2.6 stack. The real retrieval-quality numbers are produced only here."
    dashboard_config:
      - task: "Open notebooks/visual_retrieval_colab.ipynb in Colab, set runtime to L4 GPU, upload compliance.db (with pages.image_blob), Run All"
        location: "Google Colab → Runtime → Change runtime type → L4 GPU"

must_haves:
  truths:
    - "The embedder module imports offline with NO torch/colpali/qdrant installed (all heavy imports lazy inside function bodies)"
    - "The Colab notebook is a valid nbformat document that installs the pinned stack, loads vidore/colqwen2.5-v0.2 (ColQwen2_5 / ColQwen2_5_Processor), embeds each page from pages.image_blob, builds the sdf_page_images Qdrant collection, runs the text-only vs visual-fused eval, prints the metrics table, and confirms the four rq_ex3 gold pages appear in visual top-k"
    - "The notebook imports the SAME pure src/retrieval/visual builders (collection/pooling/querier/fusion/run) so plumbing is shared with the offline tests"
    - "No secrets/API keys appear in the committed notebook"
  artifacts:
    - path: "src/retrieval/visual/embedder.py"
      provides: "Lazy-import GPU seam: load ColQwen2.5, embed images/queries (notebook-only execution)"
      contains: "ColQwen2_5"
    - path: "notebooks/visual_retrieval_colab.ipynb"
      provides: "Reproducible Colab L4 deliverable: install→embed→index→eval→metrics table"
      contains: "vidore/colqwen2.5-v0.2"
  key_links:
    - from: "notebooks/visual_retrieval_colab.ipynb"
      to: "src/retrieval/visual/{collection,pooling,querier,fusion,run}.py"
      via: "import the same pure builders the offline tests cover"
      pattern: "src.retrieval.visual"
    - from: "src/retrieval/visual/embedder.py load_model"
      to: "ColQwen2_5.from_pretrained(vidore/colqwen2.5-v0.2)"
      via: "lazy import inside function body"
      pattern: "ColQwen2_5"
---

<objective>
Author the GPU embedder seam and the committed Colab L4 notebook that produces the REAL retrieval-quality numbers. The embedder is a lazy-import seam (offline-safe import; heavy work only on GPU). The notebook is the phase reproducibility deliverable: install pinned stack → load ColQwen2.5-v0.2 → embed pages from `pages.image_blob` → build the `sdf_page_images` Qdrant collection → run text-only vs visual-fused eval → print metrics table → confirm the four `rq_ex3` image-only gold pages appear in visual top-k.

Purpose: This is the no-fabrication boundary. VISUAL-01/VISUAL-02 quality numbers come ONLY from a real ColQwen2.5 run on a real GPU (CONTEXT.md §"No fabrication"). The notebook reuses the Plan 01-03 pure builders so the plumbing is identical to what the offline suite tests; only the GPU forward pass and the printed numbers are notebook-exclusive. Notebook AUTHORING + structural validation is offline-automatable; notebook EXECUTION (the numbers) is Manual-Only on Colab L4 — hence autonomous: false.
Output: `embedder.py` (lazy seam) + `notebooks/visual_retrieval_colab.ipynb` + offline structural tests, then a human Colab run.
</objective>

<execution_context>
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-CONTEXT.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-VALIDATION.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-01-PLAN.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-02-PLAN.md
@.planning/phases/05-visual-retrieval-critic-extraction/05-03-PLAN.md

<interfaces>
<!-- Contracts the executor uses directly. From RESEARCH + Plans 01-03. -->

From src/eval/ragas_quality.py:1-16,106-137 (the lazy-import seam to mirror EXACTLY):
  Module docstring: "Importing this module is offline-safe: it imports neither X nor Y
  at module load. All optional SDK imports live inside function bodies."
  All heavy imports (torch, colpali_engine, qdrant_client) go INSIDE function bodies.
From src/eval/retrieval_eval_runner.py:265-277 — graceful ImportError degradation pattern.

From RESEARCH Pattern 1 — ColQwen2.5 load + embed (notebook-only execution):
```python
import torch
from colpali_engine.models import ColQwen2_5, ColQwen2_5_Processor
model = ColQwen2_5.from_pretrained("vidore/colqwen2.5-v0.2",
            torch_dtype=torch.bfloat16, device_map="cuda:0").eval()
processor = ColQwen2_5_Processor.from_pretrained("vidore/colqwen2.5-v0.2")
batch_images = processor.process_images(images).to(model.device)
batch_queries = processor.process_queries(queries).to(model.device)
with torch.no_grad():
    image_embeddings = model(**batch_images)   # [B, seq, 128]
    query_embeddings = model(**batch_queries)
```
From RESEARCH Pattern 2 — grid via processor.get_n_patches + processor.get_image_mask (per-image dynamic grid). Confirm model.dim/model.patch_size at runtime (A2); derive dim from emb.shape[-1] if absent.

From RESEARCH §Standard Stack — pinned install cell:
```bash
pip install "colpali-engine>=0.3.11,<0.4" "transformers>=4.45,<4.50" \
            "qdrant-client>=1.17,<2.0" pypdfium2 pillow
```
RESEARCH Pitfall 4: print colpali_engine.__version__ / transformers.__version__ + smoke-embed ONE image before the full build.
RESEARCH Pitfall 3: L4, bf16, batch_size=2, torch.cuda.empty_cache()+gc.collect() between batches.
RESEARCH Pitfall 1: original vector HNSW m=0 (already in collection.build_vectors_config).
RESEARCH A1: print image_embeddings.shape[-1] to confirm dim==128 before upsert.

From Plans 01-02 (the SAME pure builders the notebook imports):
  src.retrieval.visual.collection: build_vectors_config(), collection_name(v), build_upsert_point(...)
  src.retrieval.visual.pooling: blob_to_image(blob), mean_pool_rows_cols(emb, mask, nx, ny, dim)
  src.retrieval.visual.querier: build_query_payload(...), map_response_to_candidates(points)
  src.retrieval.visual.fusion: rrf_fuse(...), to_retrieval_hits(...)
  src.retrieval.visual.run: build_visual_index_run(db_path, pages_meta, model_version, version)

From CONTEXT/RESEARCH — Example-3 proof: doc 5543408c4dacc48b, image-only pages 0,1,2,7; gold page 2 (0-indexed). The four rq_ex3_* gold queries (repaired in Plan 03) must show their gold pages in visual top-k where text recall@5 was structurally 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GPU embedder lazy-import seam (offline-safe) + offline import test</name>
  <read_first>
    - src/retrieval/visual/embedder.py (create)
    - src/eval/ragas_quality.py (analog — module docstring :1-16, lazy-import-inside-function :106-137)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md (Pattern 1 load+embed :210-233, Pattern 2 grid/pooling :235-252, Pitfall 4 smoke embed)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md (embedder.py assignment :38-64 — lazy seam, NOT a model mock)
    - tests/retrieval/visual/test_embedder_offline.py (create)
  </read_first>
  <action>
    1. Create `src/retrieval/visual/embedder.py` with the offline-safe module docstring mirroring ragas_quality.py:1-16: "Importing this module is offline-safe: it imports neither torch nor colpali_engine at module load. All heavy SDK imports live inside function bodies. This loads the REAL ColQwen2.5 model — it never mocks/fabricates embeddings (metric-integrity rule)."
    2. Implement `load_colqwen(model_id: str = "vidore/colqwen2.5-v0.2", *, dtype: str = "bfloat16", device: str = "cuda:0")` — lazy `import torch` + `from colpali_engine.models import ColQwen2_5, ColQwen2_5_Processor` INSIDE the body; return `(model, processor)` with `ColQwen2_5.from_pretrained(model_id, torch_dtype=getattr(torch, dtype), device_map=device).eval()` and `ColQwen2_5_Processor.from_pretrained(model_id)`.
    3. Implement `embed_images(model, processor, images)` and `embed_queries(model, processor, queries)` per Pattern 1 (lazy torch, `with torch.no_grad()`, `process_images`/`process_queries`, `.to(model.device)`). Implement `pooled_vectors_for_image(model, processor, batch_images, image_embeddings, i, image_size)` that calls `processor.get_n_patches(...)` + `processor.get_image_mask(...)` then delegates the reshape to `pooling.mean_pool_rows_cols(...)` (Plan 02) — derive dim from `image_embeddings[i].shape[-1]` (A2). These functions are EXECUTED only on GPU (notebook); no offline test asserts a quality metric off them.
    4. Create `tests/retrieval/visual/test_embedder_offline.py`: assert the module imports with NO torch/colpali installed (`venv\Scripts\python.exe -c "import src.retrieval.visual.embedder"` style, or `importlib.import_module`), and assert that calling `load_colqwen()` WITHOUT the deps raises ImportError (lazy seam — proves no module-top heavy import). Mark any GPU-exercising test `@pytest.mark.gpu` (registered in Plan 01) so it is skipped offline. NO offline test fabricates an embedding or asserts recall/ndcg.
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_embedder_offline.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep `ColQwen2_5` and `vidore/colqwen2.5-v0.2` in embedder.py succeed.
    - grep `import torch` and `from colpali_engine` in embedder.py appear ONLY inside function bodies (no module-top); `venv\Scripts\python.exe -c "import src.retrieval.visual.embedder"` succeeds with no torch/colpali installed.
    - grep `mean_pool_rows_cols` in embedder.py succeeds (reuses the Plan 02 pure pooler, not a re-implementation).
    - The offline test contains a `@pytest.mark.gpu` guard on any model-execution test and an import-safety assertion.
    - `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_embedder_offline.py -x -q` passes.
  </acceptance_criteria>
  <done>embedder.py loads the REAL ColQwen2.5 via a lazy-import seam (offline-importable; load raises ImportError without deps); image/query embedding + pooled-vector extraction reuse the Plan 02 pure pooler; GPU tests are skip-guarded; offline import test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Colab L4 notebook + offline structural validation test</name>
  <read_first>
    - notebooks/visual_retrieval_colab.ipynb (create — new notebooks/ dir; no existing .ipynb in repo)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-RESEARCH.md (Architecture diagram :146-183, install cell :129-134, Pitfalls 1/3/4, A1; Example-3 proof :98)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-PATTERNS.md (notebook assignment :240-242)
    - .planning/phases/05-visual-retrieval-critic-extraction/05-VALIDATION.md (Manual-Only Colab verification :61-68)
    - src/eval/retrieval_eval_runner.py (the eval the notebook re-runs for text-only vs visual-fused)
    - tests/retrieval/visual/test_notebook_structure.py (create)
  </read_first>
  <action>
    1. Create `notebooks/visual_retrieval_colab.ipynb` as a valid nbformat v4 JSON document (use the Write tool to emit well-formed notebook JSON, OR a python authoring script run once that writes it via `nbformat`; either way the committed artifact is the .ipynb). Cells, in order, with clear markdown headers:
       - **md** Title + purpose + "produces the real VISUAL-01/VISUAL-02 numbers; reproducible on Colab Pro L4".
       - **code** Pinned install cell EXACTLY per RESEARCH: `pip install "colpali-engine>=0.3.11,<0.4" "transformers>=4.45,<4.50" "qdrant-client>=1.17,<2.0" pypdfium2 pillow`.
       - **code** Version print + smoke embed: print `colpali_engine.__version__`, `transformers.__version__`, `torch.__version__`; load model/processor via `src.retrieval.visual.embedder.load_colqwen`; embed ONE image; `print(image_embeddings.shape)` to confirm dim (A1, Pitfall 4) BEFORE the full build.
       - **md/code** Mount/upload `compliance.db` (the input with pages.image_blob); read pages via parameterized SQL `SELECT doc_id, page_num, filename, image_blob FROM pages JOIN documents USING(doc_id)` — decode each via `pooling.blob_to_image` (Pattern: rasterize from image_blob, NOT source PDFs).
       - **code** Build the Qdrant collection: `client = QdrantClient(path="/content/qdrant_storage")`; `client.create_collection(collection_name=collection_name(1), vectors_config=build_vectors_config())` (imports the Plan 01 pure builders). Embed pages in batches (bf16, batch_size=2, `torch.cuda.empty_cache()`+`gc.collect()` between batches — Pitfall 3); for each page compute pooled rows/cols via `embedder.pooled_vectors_for_image` and upsert via `collection.build_upsert_point` (0-indexed page_num payload). Persist the run via `run.build_visual_index_run`.
       - **code** Two-stage retrieval over the gold set: embed each (repaired) gold query, `client.query_points(**build_query_payload(...))`, `map_response_to_candidates`, then `rrf_fuse` with the text-tier ranked hits + `to_retrieval_hits`.
       - **code** Run the eval BOTH modes on the SAME gold set: text-only (existing path) vs visual-fused; compute recall@5/@10, ndcg@5, citation accuracy using the existing `src/eval` metric functions (UNCHANGED). Print a metrics TABLE comparing the two modes.
       - **code** Example-3 proof cell: for each of the four `rq_ex3_*` queries, print whether the gold page (doc 5543408c4dacc48b, page 2 0-indexed, and the other rq_ex3 targets) appears in the visual/fused top-k — demonstrating image-only pages are now retrievable where text recall@5 was 0.
       - **md** Persist/export note: zip `/content/qdrant_storage` + a manifest (collection name, run_id, point count, model version) for the deferred demo (RESEARCH Open Q2).
    2. SECURITY: the notebook contains NO API keys / secrets (retrieval is provider-free; GEMINI_API_KEY/Langfuse not needed). Do NOT hardcode any key; if HF auth is ever needed use `from google.colab import userdata`. Image bytes and page text stay in-memory; only counts/scores/run_ids are printed (no full page text dumped).
    3. Create `tests/retrieval/visual/test_notebook_structure.py` (OFFLINE, no GPU): parse the .ipynb with `nbformat.read` (assert valid v4); assert the install cell pins `colpali-engine`, `transformers`, `qdrant-client`; assert source contains the required tokens — `vidore/colqwen2.5-v0.2`, `build_vectors_config`, `build_query_payload`, `rrf_fuse`, `collection_name`, `rq_ex3`; assert NO secret-shaped strings (grep the concatenated source for `sk-`, `AIza`, `LANGFUSE_SECRET_KEY=`, etc. → must be ABSENT). This validates AUTHORING only — it does NOT execute the notebook or assert any retrieval-quality number (that is Manual-Only Colab).
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_notebook_structure.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/visual_retrieval_colab.ipynb` exists and `nbformat.read` parses it as valid v4 (test asserts this).
    - grep `vidore/colqwen2.5-v0.2`, `colpali-engine>=0.3.11,<0.4`, `build_vectors_config`, `build_query_payload`, `rrf_fuse`, `rq_ex3` in the notebook JSON all succeed.
    - grep for secret patterns (`sk-`, `AIza`, `_SECRET_KEY=`) in the notebook returns NOTHING (test asserts absence).
    - grep `src.retrieval.visual` in the notebook succeeds (it imports the shared pure builders, not re-implementations).
    - `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_notebook_structure.py -x -q` passes.
  </acceptance_criteria>
  <done>notebooks/visual_retrieval_colab.ipynb is a valid nbformat document that installs the pinned stack, smoke-checks versions/dim, embeds pages from image_blob, builds the 3-named-vector sdf_page_images collection, runs two-stage retrieval, evaluates text-only vs visual-fused, prints the metrics table, and proves the rq_ex3 image-only gold pages appear in visual top-k — importing the shared pure builders and carrying no secrets; offline structural test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual-Only Colab L4 run — produce the real VISUAL-01/VISUAL-02 numbers</name>
  <action>HUMAN-RUN checkpoint (no autonomous code action). The human executes the committed notebook on Colab Pro L4 to produce the real retrieval-quality numbers — the no-fabrication boundary. Numbers are NOT runnable in offline CI; they are recorded into the SUMMARY. Exact steps are in the how-to-verify block below.</action>
  <what-built>The reproducible Colab L4 deliverable: notebooks/visual_retrieval_colab.ipynb (authored + structurally validated offline) plus the embedder GPU seam. This checkpoint covers the EXECUTION of the notebook on real hardware — the no-fabrication boundary where the real VISUAL-01/VISUAL-02 numbers are produced.</what-built>
  <how-to-verify>
    1. Open `notebooks/visual_retrieval_colab.ipynb` in Google Colab.
    2. Runtime → Change runtime type → L4 GPU. Run the version/smoke-embed cell FIRST; confirm colpali-engine/transformers/torch versions print and the one-image embedding shape ends in the per-token dim (expect 128 — RESEARCH A1). If colpali-engine 0.3.x conflicts with transformers<4.50 (RESEARCH Pitfall 4), pin the exact loadable pair and note it in the notebook.
    3. Upload `compliance.db` (the local file WITH pages.image_blob; NEVER commit it). Run All.
    4. Confirm the Qdrant collection builds without OOM (batch_size=2, bf16; Pitfall 3) and indexed_vectors_count is sane (original HNSW disabled; Pitfall 1).
    5. Read the printed metrics TABLE: text-only vs visual-fused recall@5/@10, ndcg@5, citation accuracy on the gold set.
    6. Confirm the Example-3 proof cell shows the four rq_ex3_* gold pages (incl. doc 5543408c4dacc48b page 2, 0-indexed, image-only) appearing in the visual/fused top-k — i.e. recall lift above the 0.647 text-only ceiling on those queries.
    7. Record the real numbers + the exact (colpali-engine, transformers, torch) versions that loaded into the SUMMARY. Zip /content/qdrant_storage + manifest as the build artifact.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded metrics table + version pair, or describe issues (OOM, version conflict, rq_ex3 pages NOT appearing) for follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compliance.db (image_blob) → Colab runtime | Local file uploaded to ephemeral Colab; never committed; image bytes stay in-memory |
| Committed notebook → public repo | Must contain NO secrets/API keys |
| GPU embedder seam import on local box | Must not load torch/colpali at module import (Blackwell-incompatible stack) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure | committed notebook secrets | mitigate | Retrieval is provider-free; notebook hardcodes no keys; test asserts absence of sk-/AIza/_SECRET_KEY patterns; HF auth (if needed) via colab userdata |
| T-05-17 | Information Disclosure | page text / image bytes in notebook output | mitigate | Notebook prints counts/scores/run_ids/metrics only; image bytes + page text stay in-memory; no full-text dump |
| T-05-18 | Denial of Service | embedder import loads Blackwell-incompatible torch locally | mitigate | All heavy imports lazy inside function bodies; offline import test proves module loads without torch/colpali |
| T-05-19 | Spoofing | fabricated quality numbers | mitigate | Numbers come ONLY from the real Colab GPU run; offline tests validate STRUCTURE only and never assert recall/ndcg; checkpoint records real numbers + version pair |
| T-05-20 | Information Disclosure | exported Qdrant artifact | accept | Local/Drive artifact only; contains embeddings + identifier payloads (no page text/secrets); demo deferred |
</threat_model>

<verification>
- Offline (automatable): `venv\Scripts\python.exe -m pytest tests/retrieval/visual/test_embedder_offline.py tests/retrieval/visual/test_notebook_structure.py -x -q` — embedder imports without heavy deps; notebook parses as valid nbformat with required cells + no secrets.
- Manual-Only (Colab L4): the notebook end-to-end run prints the real metrics table and the rq_ex3 proof. This is the no-fabrication boundary — NOT runnable in offline CI; numbers recorded in the SUMMARY.
- Full offline suite green: `venv\Scripts\python.exe -m pytest -q`.
</verification>

<success_criteria>
- embedder.py loads the real ColQwen2.5 via a lazy seam (offline-importable; reuses the Plan 02 pooler); offline import test green.
- notebooks/visual_retrieval_colab.ipynb is valid nbformat, installs the pinned stack, embeds from image_blob, builds the 3-named-vector sdf_page_images collection, evaluates text-only vs visual-fused, prints the metrics table, and proves rq_ex3 image-only pages appear in visual top-k — importing the shared pure builders, carrying no secrets.
- Offline structural test green; the Colab L4 execution (Manual-Only) produces and records the real VISUAL-01/VISUAL-02 numbers + the loadable (colpali-engine, transformers, torch) version pair.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-retrieval-critic-extraction/05-04-SUMMARY.md` recording the real Colab metrics table, the rq_ex3 proof outcome, and the exact version pair that loaded.
</output>
