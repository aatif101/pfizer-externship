---
phase: quick-260611-ou3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/retrieval/models.py
  - src/retrieval/retriever.py
  - src/rag/models.py
  - src/rag/service.py
  - src/rag/gemini.py
  - src/eval/ragas_quality.py
  - tests/test_retriever.py
  - tests/test_answer_service.py
  - tests/test_answer_provider_gemini.py
  - tests/eval/test_ragas_quality.py
autonomous: false
requirements: [QUICK-260611-OU3]
user_setup: []

must_haves:
  truths:
    - "Each strong RetrievalHit carries bounded evidence_text sourced from full page_text (cap ~2000 chars, word-boundary truncation), falling back to indexed_snippet when page_text is empty"
    - "The Gemini generator prompt embeds evidence_text (not the 160-221 char teaser snippet)"
    - "RAGAS judge contexts are EXACTLY the evidence_text strings the generator saw — the faithfulness contract"
    - "The short indexed snippet still flows to AnswerCitation.snippet so dashboard chat citation rendering is unchanged"
    - "evidence_text lives in memory only — never persisted to rag_eval_observations, never in Langfuse metadata allowlists"
    - "Full offline test suite stays green; new tests prove truncation, fallback, generator-prompt content, and judge==generator context equality"
  artifacts:
    - path: "src/retrieval/retriever.py"
      provides: "evidence_text built from page_text with word-boundary truncation + snippet fallback"
      contains: "evidence_text"
    - path: "src/rag/gemini.py"
      provides: "evidence blocks use hit.evidence_text"
      contains: "evidence_text"
    - path: "src/eval/ragas_quality.py"
      provides: "RagasSample.contexts built from citation.evidence_text"
      contains: "evidence_text"
  key_links:
    - from: "src/retrieval/retriever.py::_score_candidate"
      to: "RetrievalHit.evidence_text"
      via: "bounded page_text builder"
      pattern: "evidence_text="
    - from: "src/rag/gemini.py::_build_contents"
      to: "hit.evidence_text"
      via: "evidence block string"
      pattern: "evidence_text"
    - from: "src/eval/ragas_quality.py::compute_ragas_quality"
      to: "citation.evidence_text"
      via: "RagasSample.contexts"
      pattern: "evidence_text"
---

<objective>
RAGAS faithfulness is 0.000 on all 13 answered gold queries because the judge's
contexts are the 160-221 char indexed teaser snippets (truncated mid-word), and
the generator is starved on the same teasers. The retriever already loads full
page text internally (`COALESCE(p.page_text,'')` in both FTS and lexical SQL) —
it just never exposes it publicly.

Widen the public evidence path: add a bounded `evidence_text` (sourced from
`page_text`, cap ~2000 chars, word-boundary truncation, fallback to the indexed
snippet for empty/scanned pages) to `RetrievalHit` and `AnswerCitation`. The
generator and the RAGAS judge both consume `evidence_text`; the short `snippet`
stays untouched for dashboard display. Then run the live before/after eval and
report honest numbers.

Purpose: Give the generator real grounding and give the faithfulness judge the
exact contexts the generator saw — the only correct faithfulness contract.
Output: Wider evidence in memory, unchanged display citations, unchanged privacy
boundary, green offline suite, and a before/after eval report in the SUMMARY.
</objective>

<execution_context>
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/templates/summary.md

WINDOWS HARD RULE (CLAUDE.md): never run verification via bash or /bin/bash.
Use `venv\Scripts\python.exe -m pytest ...` only. compliance.db and .env are
NEVER staged. Executor runs on main with no worktree isolation — atomic commits.
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Shared DTOs the evidence flows through. evidence_text must be ADDITIVE. -->

From src/retrieval/models.py — RetrievalHit (frozen dataclass, fields are
constructed positionally in tests, so add evidence_text as a NEW field):
```python
@dataclass(frozen=True)
class RetrievalHit:
    doc_id: str
    filename: str
    page_num: int
    display_page_num: int
    score: float
    score_components: RetrievalScoreComponents
    snippet: str
    # ADD: evidence_text: str = ""   (bounded page text; defaults to "" so
    # existing positional constructions in tests stay valid)
```

From src/rag/models.py — AnswerCitation (frozen dataclass):
```python
@dataclass(frozen=True)
class AnswerCitation:
    doc_id: str
    filename: str
    page_num: int
    display_page_num: int
    snippet: str
    score: float
    # ADD: evidence_text: str = ""   (in-memory only; NOT rendered by dashboard)
```

From src/rag/service.py:
- answer_question builds AnswerProviderRequest(question, run_id, evidence=evidence.hits)
  then provider.answer(request) -> _citations_from_hits(evidence.hits[:k])
- _citations_from_hits maps each hit -> AnswerCitation(..., snippet=hit.snippet, ...)
  MUST also pass evidence_text=hit.evidence_text

From src/rag/gemini.py:
- _MAX_SNIPPET_CHARS=600, _MAX_EVIDENCE_ITEMS=5
- _build_contents iterates request.evidence[:_MAX_EVIDENCE_ITEMS] and emits
  _bounded_snippet(hit.snippet) inside each <evidence> block
  MUST switch to hit.evidence_text (with a wider bound, e.g. _MAX_EVIDENCE_CHARS)

From src/eval/ragas_quality.py (line ~223):
- RagasSample(contexts=tuple(citation.snippet for citation in result.citations), ...)
  MUST become tuple(citation.evidence_text for citation in result.citations)

From src/retrieval/retriever.py:
- _Candidate already has page_text; _score_candidate computes
  text = normalize_index_text(candidate.page_text) or candidate.indexed_snippet
  and builds snippet=make_query_snippet(text, terms).
  ADD evidence_text=_bounded_evidence_text(text, fallback=indexed_snippet).
- _RETRIEVAL_TRACE_ALLOWED_KEYS allowlist (retriever) and
  _ANSWER_TRACE_ALLOWED_KEYS allowlist (service) MUST stay unchanged — no
  evidence_text key added to either.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded evidence_text to retrieval + answer DTOs and producers</name>
  <files>src/retrieval/models.py, src/retrieval/retriever.py, src/rag/models.py, src/rag/service.py, tests/test_retriever.py, tests/test_answer_service.py</files>
  <behavior>
    - evidence_text builder: page_text >2000 chars truncates at the LAST word
      boundary at/under the cap and appends an ellipsis; never splits mid-word.
    - page_text <= cap returns the normalized text verbatim (no ellipsis).
    - Empty/whitespace page_text falls back to the indexed_snippet string.
    - RetrievalHit gains evidence_text (default "") — existing positional
      RetrievalHit(...) constructions in tests still compile.
    - AnswerCitation gains evidence_text (default ""); _citations_from_hits
      copies hit.evidence_text onto the citation.
    - The short snippet is byte-for-byte unchanged on every hit (regression guard).
    - Privacy: evidence_text never appears in _RETRIEVAL_TRACE_ALLOWED_KEYS.
  </behavior>
  <action>
    Add `evidence_text: str = ""` as the final field of RetrievalHit
    (src/retrieval/models.py) and AnswerCitation (src/rag/models.py) — default ""
    so existing positional constructions stay valid.

    In src/retrieval/retriever.py: add a module-level `_MAX_EVIDENCE_TEXT_CHARS = 2000`
    and a pure helper `_bounded_evidence_text(text: str, *, fallback: str) -> str`:
    normalize the candidate page text; if empty/whitespace after normalization,
    return `normalize_index_text(fallback)`; if len <= cap return it verbatim;
    else slice to the cap, then `rsplit(" ", 1)[0]` to drop the partial trailing
    word (guard against a single >cap token: if no space, hard-slice at cap),
    rstrip, append "…". In _score_candidate, after computing
    `text = normalize_index_text(candidate.page_text) or candidate.indexed_snippet`,
    set `evidence_text=_bounded_evidence_text(candidate.page_text, fallback=candidate.indexed_snippet)`
    on the returned RetrievalHit. Leave `snippet=make_query_snippet(text, terms)`
    and the trace allowlist exactly as-is.

    In src/rag/service.py::_citations_from_hits, add
    `evidence_text=hit.evidence_text` to each AnswerCitation. Do not touch the
    answer-trace allowlist.

    Tests: in tests/test_retriever.py add unit tests for _bounded_evidence_text
    (long page_text -> word-boundary truncation + ellipsis, no mid-word cut;
    short text verbatim; empty page_text -> snippet fallback) and assert a strong
    hit's evidence_text contains text beyond the 222-char snippet bound while the
    snippet itself is unchanged. In tests/test_answer_service.py assert the
    answered-path citation carries evidence_text equal to the originating hit's
    evidence_text. Update any test that constructs RetrievalHit/AnswerCitation
    positionally only if the new defaulted trailing field breaks it (it should not).
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/test_retriever.py tests/test_answer_service.py -x -q</automated>
  </verify>
  <done>RetrievalHit and AnswerCitation expose evidence_text; retriever populates it from page_text with word-boundary truncation and snippet fallback; service copies it onto citations; snippet unchanged; trace allowlists unchanged; both test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed evidence_text to the Gemini generator and the RAGAS judge</name>
  <files>src/rag/gemini.py, src/eval/ragas_quality.py, tests/test_answer_provider_gemini.py, tests/eval/test_ragas_quality.py</files>
  <behavior>
    - The generator prompt's <evidence> blocks contain each hit's evidence_text
      (bounded to a wider cap, e.g. 2000 chars), NOT the short snippet.
    - _MAX_EVIDENCE_ITEMS stays 5.
    - RagasSample.contexts == the evidence_text strings of the answered
      citations, in order — the faithfulness contract. A test proves the judge's
      contexts equal the strings the generator received for the same hits.
    - An empty evidence_text in a hit degrades gracefully (block still renders;
      falls back to snippet only if you choose — but keep it simple: render
      evidence_text as-is since the retriever already guarantees a non-empty
      fallback).
  </behavior>
  <action>
    In src/rag/gemini.py: rename/retune the bound to a wider evidence cap
    (introduce `_MAX_EVIDENCE_CHARS = 2000`; keep `_MAX_SNIPPET_CHARS` only if
    still referenced, otherwise remove it). In _build_contents, change the
    per-block payload from `_bounded_snippet(hit.snippet)` to
    `_bounded_evidence(hit.evidence_text or hit.snippet)` where _bounded_evidence
    caps at _MAX_EVIDENCE_CHARS with the same trailing-ellipsis behavior. Keep
    `request.evidence[:_MAX_EVIDENCE_ITEMS]` (5).

    In src/eval/ragas_quality.py (line ~223): change
    `contexts=tuple(citation.snippet for citation in result.citations)` to
    `contexts=tuple(citation.evidence_text for citation in result.citations)`.
    Do NOT change the persistence path — only numeric scores + ids + status are
    written; evidence_text stays in memory (T-mw5-01 boundary intact).

    Tests: in tests/test_answer_provider_gemini.py assert that the contents string
    passed to the fake client contains a hit's evidence_text payload and does not
    rely on the short snippet (construct a RetrievalHit whose evidence_text differs
    from its snippet). In tests/eval/test_ragas_quality.py assert that, for an
    answered query, the RagasSample.contexts handed to the injected scorer equal
    the evidence_text values of result.citations (capture via a fake scorer/
    answer_fn). Confirm no evidence_text string is written to rag_eval_observations.
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest tests/test_answer_provider_gemini.py tests/eval/test_ragas_quality.py tests/test_dashboard_chat_tab.py tests/test_chat_dashboard.py -x -q</automated>
  </verify>
  <done>Generator prompt embeds evidence_text; RAGAS contexts equal the answered citations' evidence_text; dashboard chat citation rendering tests still pass (snippet path untouched); persistence still writes only numeric scores.</done>
</task>

<task type="auto">
  <name>Task 3: Full offline suite green, then run live before/after eval</name>
  <files>(no source changes — validation + reporting only)</files>
  <action>
    1. Run the FULL offline suite and confirm it stays green (was 323):
       `venv\Scripts\python.exe -m pytest -q`
       Fix any incidental breakage from the new defaulted fields before proceeding.

    2. Record BEFORE numbers (already in DB from run df3762889b754af79aefe8314ad1e245)
       into the SUMMARY: faithfulness avg 0.000 (13 observed), answer_relevancy
       avg ~0.78, recall@5 0.647, 4 rq_ex3_* below_threshold, probe answer for
       rq_ex5_vendor = "The supplied evidence is insufficient." Read these from
       compliance.db via the eval repository / CLI rather than retyping blindly.

    3. Run the AFTER live eval (paid Gemini key in .env, cost approved):
       `venv\Scripts\python.exe -m src.eval.cli run --db-path compliance.db --with-ragas --include-latency-cost`

    4. Re-probe the generator on rq_ex5_vendor (strong_evidence) and capture the
       new answer text (it should no longer say "insufficient" if evidence now
       supports it).

    5. In the SUMMARY, report a per-query side-by-side table (faithfulness +
       answer_relevancy before vs after), the new averages, recall@5 and citation
       accuracy, and the re-probed answer. HONEST reporting: a still-low post-fix
       faithfulness is a finding, not a failure — explain what the numbers show.

    Do NOT stage compliance.db or .env in any commit.
  </action>
  <verify>
    <automated>venv\Scripts\python.exe -m pytest -q</automated>
  </verify>
  <done>Full offline suite green; BEFORE numbers recorded from DB; AFTER live eval run completed with --with-ragas; rq_ex5_vendor re-probed; before/after per-query table + averages + honest interpretation captured in SUMMARY; compliance.db and .env never staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retriever → public DTO | Full page_text crosses into RetrievalHit.evidence_text — must stay bounded and never enter trace/persistence allowlists |
| answer service → Langfuse / rag_eval_observations | evidence_text must NOT leak into metadata or persisted rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ou3-01 | Information disclosure | RetrievalHit.evidence_text → Langfuse metadata | mitigate | Leave _RETRIEVAL_TRACE_ALLOWED_KEYS and _ANSWER_TRACE_ALLOWED_KEYS unchanged; never add an evidence_text key. Existing allowlist already drops unknown keys. |
| T-ou3-02 | Information disclosure | evidence_text → rag_eval_observations rows | mitigate | RagasSample is in-memory only (existing T-mw5-01 contract); persistence path writes only numeric scores + ids + status. Add a test asserting no evidence_text string is persisted. |
| T-ou3-03 | Denial of service | Unbounded page_text inflating prompt/judge cost | mitigate | Cap evidence_text at ~2000 chars per hit with word-boundary truncation; _MAX_EVIDENCE_ITEMS stays 5. |
| T-ou3-04 | Tampering | Display citation snippet silently widened, breaking dashboard | accept | snippet field left byte-for-byte unchanged; regression test asserts snippet bound <= 222. Dashboard renders snippet only. |
</threat_model>

<verification>
- Offline: `venv\Scripts\python.exe -m pytest -q` green (>= 323 + new tests).
- evidence_text present on strong hits and citations; snippet unchanged.
- Generator prompt contains evidence_text; judge contexts == generator evidence.
- No evidence_text in trace allowlists or persisted eval rows.
- Live AFTER eval run completes; before/after numbers captured honestly.
</verification>

<success_criteria>
- Both the Gemini generator and the RAGAS judge consume bounded evidence_text
  sourced from full page_text (word-boundary truncation, snippet fallback).
- Dashboard chat citation rendering is unchanged (snippet path untouched).
- Privacy boundary intact: evidence_text in memory only.
- Full offline suite green offline (no network).
- SUMMARY contains a before/after per-query faithfulness/relevancy table,
  averages, recall@5/citation accuracy, the re-probed rq_ex5_vendor answer, and
  an honest interpretation of the post-fix numbers.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-ou3-widen-rag-evidence-from-truncated-index-/260611-ou3-SUMMARY.md`
</output>
