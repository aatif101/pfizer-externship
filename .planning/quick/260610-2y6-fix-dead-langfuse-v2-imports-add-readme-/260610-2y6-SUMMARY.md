---
phase: quick-260610-2y6
plan: 01
subsystem: observability, docs
tags: [langfuse, tracing, readme, housekeeping]
requires: []
provides:
  - Live-capable Langfuse v3 tracing seam in retriever/indexer/service/pipeline
  - Root README.md (purpose, architecture, setup, demo flow, retrieval tiers)
  - Clean repo root (pip junk and 0-byte db files removed)
affects: [src/tracing.py, src/retrieval, src/rag, src/extraction]
tech-stack:
  added: []
  patterns:
    - "Module-level injectable seams (_LANGFUSE_AVAILABLE / langfuse_context) with lazy get_client() fallback in src.tracing"
key-files:
  created:
    - README.md
  modified:
    - src/tracing.py
    - src/retrieval/retriever.py
    - src/retrieval/indexer.py
    - src/rag/service.py
    - src/extraction/pipeline.py
  deleted:
    - "=2.72.0,"
decisions:
  - "Reworded tracing.py docstring to describe the dead v2 decorators path without the literal 'langfuse.decorators' string, so the repo-wide grep done-criterion (zero matches in src/) holds"
metrics:
  duration: "~8 minutes"
  completed: "2026-06-10"
  tasks: 3
  tests: "303 passed (full suite, offline)"
---

# Quick Task 260610-2y6: Fix Dead Langfuse v2 Imports, Add README, Clean Root — Summary

Replaced the nonexistent `langfuse.decorators` v2 import path in four modules with the working v3 `get_client()` seam from src/tracing.py (tracing now actually fires when keys are present), added a root README documenting the full demo flow and retrieval-tier rationale, and removed root junk files.

## What Was Done

### Task 1: Langfuse v3 client seam (commit 89ba8a2)

- **src/tracing.py:** `_get_langfuse_context()` now lazily returns the live v3 client via `get_client()` (try/except → None when the SDK is absent). Module docstring corrected: v3 paths are `from langfuse import observe, get_client`; the legacy decorators submodule is documented as dead. Verified `_get_langfuse_context()` returns `langfuse._client.client.Langfuse` with the SDK installed (disabled no-op client without keys).
- **src/retrieval/retriever.py, src/retrieval/indexer.py, src/rag/service.py:** deleted the dead `try: from langfuse.decorators import ...` blocks and local no-op `observe` defs; now import `observe`/`safe_update_current_trace` from `src.tracing`. Kept both module-level test seams (`_LANGFUSE_AVAILABLE: bool = True`, `langfuse_context: Any | None = None`). Hoisted inline allowlists into `_RETRIEVAL_TRACE_ALLOWED_KEYS` / `_INDEX_TRACE_ALLOWED_KEYS` / `_ANSWER_TRACE_ALLOWED_KEYS` frozensets (exact same keys). Each `_safe_update_trace_metadata` now routes through `safe_update_current_trace(tags=..., metadata=..., allowed_metadata_keys=..., context=langfuse_context)` reading the seam globals at call time.
- **src/extraction/pipeline.py:** replaced the dead import block with a plain `langfuse_context: Any | None = None` seam; it already routed through `safe_update_current_trace(..., context=langfuse_context)`.
- Per-module tags unchanged: `["retrieval", "evidence"]`, `["retrieval", "index"]`, `["rag", "answer"]`.
- `grep -rn "langfuse.decorators" src/` → zero matches.

### Task 2: README + root cleanup (commit 571bab2)

- `git rm -- "=2.72.0,"` (tracked 0-byte pip-quoting accident).
- Deleted three untracked 0-byte files: `empty-dashboard.db`, `populated-dashboard.db`, `sdf_intel.db`.
- `compliance.db` (27 MB), `private/`, `local_data/` untouched.
- Wrote `README.md` (78 lines): purpose, architecture table, setup (venv, `pip install -e .`, `.env` with `GEMINI_API_KEY` and optional Langfuse keys), exact verified demo commands (ingest → retrieval build/status → extract-all / --visual-fallback / extract --doc-id → `streamlit run src/app.py`), retrieval tiers (Tier 1 deterministic FTS5+lexical for auditability; Tier 2 Qdrant+ColQwen2.5 deferred behind the same evidence gate), testing, and data hygiene.

### Task 3: Full-suite regression

- `venv/Scripts/python.exe -m pytest -q` → **303 passed** in 2:38, fully offline, no API keys.

## Behavior Matrix Verified (test seams unchanged)

- `_LANGFUSE_AVAILABLE=False, langfuse_context=None` → no-op (test passes)
- `_LANGFUSE_AVAILABLE=True, langfuse_context=fake` → fake records filtered tags+metadata updates (`index_status=built`, `evidence_reason=strong_evidence`, `answer_status=answered`, `trace_id=trace-safe-001` all asserted)
- `_LANGFUSE_AVAILABLE=True, langfuse_context=failing-fake` → exception swallowed, zero updates
- Production defaults (`True`/`None`) → lazy `_get_langfuse_context()` → live v3 client (disabled no-op offline)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Docstring wording vs. grep done-criterion conflict**
- **Found during:** Task 1, step 4 (repo-wide sweep)
- **Issue:** Plan step 1 asked the docstring to name `langfuse.decorators` as the dead path, but step 4 / done criterion require zero grep matches for that string in src/.
- **Fix:** Reworded the docstring to describe the legacy decorators submodule without the literal dotted string. Intent preserved, grep criterion satisfied.
- **Files modified:** src/tracing.py
- **Commit:** 89ba8a2

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | 89ba8a2 | fix(quick-260610-2y6): replace dead Langfuse v2 imports with v3 client seam |
| 2 | 571bab2 | chore(quick-260610-2y6): add root README and remove pip junk file |
| 3 | (none) | verification only — 303 passed |

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes; tracing allowlists and secret-filtering behavior unchanged.

## Self-Check: PASSED

- README.md exists at repo root (78 lines, all required sections)
- src/tracing.py contains `get_client()` in `_get_langfuse_context`
- Commits 89ba8a2 and 571bab2 present in git log
- `grep -rn "langfuse.decorators" src/` → no matches
- Root junk gone; compliance.db intact at ~27 MB
- Full suite: 303 passed offline
