---
phase: quick-260610-2y6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tracing.py
  - src/retrieval/retriever.py
  - src/retrieval/indexer.py
  - src/rag/service.py
  - src/extraction/pipeline.py
  - README.md
  - "=2.72.0,"
  - empty-dashboard.db
  - populated-dashboard.db
  - sdf_intel.db
autonomous: true
requirements: [QUICK-260610-2y6]

must_haves:
  truths:
    - "No module in src/ imports langfuse.decorators (dead v2 path under installed v3)"
    - "With Langfuse keys configured, retriever/indexer/service/pipeline trace metadata reaches the live v3 client via get_client()"
    - "With no Langfuse keys (current offline state), all code paths remain no-op-safe and all 303 tests pass"
    - "README.md at repo root documents purpose, architecture, setup, demo CLI flow, and retrieval tiers"
    - "Tracked junk file '=2.72.0,' and the three 0-byte db files are gone; compliance.db, private/, local_data/ untouched"
  artifacts:
    - path: "README.md"
      provides: "Project overview, setup, demo flow, retrieval tiers rationale"
      min_lines: 60
    - path: "src/tracing.py"
      provides: "_get_langfuse_context() returning live v3 client via get_client()"
      contains: "get_client()"
  key_links:
    - from: "src/retrieval/retriever.py"
      to: "src/tracing.py"
      via: "from src.tracing import observe, safe_update_current_trace"
      pattern: "from src\\.tracing import"
    - from: "src/tracing.py"
      to: "langfuse v3 client"
      via: "lazy get_client() in _get_langfuse_context"
      pattern: "get_client\\(\\)"
---

<objective>
Three housekeeping fixes in one pass: (1) replace dead Langfuse v2 import paths (`langfuse.decorators`) with the working v3 client seam already proven in src/tracing.py, so `@observe` decorators and trace-metadata updates actually fire when Langfuse keys are present; (2) add a root README.md documenting the system; (3) remove root junk (tracked `=2.72.0,` pip accident, three 0-byte untracked .db files).

Purpose: The installed langfuse is v3; `langfuse.decorators` does not exist (verified ModuleNotFoundError), so four modules' tracing silently no-ops. Repo also lacks a README and has junk at root.
Output: Live-capable tracing seam, README.md, clean root. All 303 tests still pass offline.
</objective>

<execution_context>
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/smati/VS Code Projects/pfizer-externship/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md  (Windows verification rule: use venv/Scripts/python.exe -m pytest, never /bin/bash)
@src/tracing.py
@src/retrieval/retriever.py
@src/retrieval/indexer.py
@src/rag/service.py
@src/extraction/pipeline.py
@tests/test_tracing.py

<interfaces>
Working v3 API already in src/tracing.py (do NOT re-derive):

```python
from langfuse import observe, get_client   # v3 — works; tracing.py wraps both in try/except with no-op fallbacks

def safe_update_current_trace(
    *,
    tags: Iterable[str] | None = None,
    metadata: Mapping[str, Any] | None = None,
    allowed_metadata_keys: set[str] | frozenset[str] | None = None,
    context: Any | None = None,           # injected context with .update_current_trace; None → _get_langfuse_context() fallback
) -> bool: ...
```

Test seams that MUST keep working (verified by grep):
- tests/test_tracing.py lines 288-350: monkeypatches BOTH `_LANGFUSE_AVAILABLE` and `langfuse_context` as module-level attrs on `src.retrieval.indexer`, `src.retrieval.retriever`, `src.rag.service`. Fake context records `update_current_trace(**kwargs)` calls; tests assert allowlisted keys appear (`index_status=built`, `evidence_reason=strong_evidence`, `answer_status=answered`, `trace_id=trace-safe-001`) and forbidden content never appears.
- tests/test_tracing.py line 364: monkeypatches `service._LANGFUSE_AVAILABLE` / `service.langfuse_context` alone.
- tests/test_extraction_pipeline.py lines 264-401: monkeypatches `pipeline.langfuse_context` (module-level symbol must remain).
- All direct `safe_update_current_trace` tests (test_tracing.py lines 67-260) inject `context=` explicitly — changing the `_get_langfuse_context()` fallback cannot affect them.

Current per-module allowlists and tags (preserve EXACTLY):
- retriever.py: tags `["retrieval", "evidence"]`, keys {boundary, run_id, evidence_reason, top_score, citation_count, is_strong, error_class}
- indexer.py: tags `["retrieval", "index"]`, keys {boundary, index_status, run_id, source_document_count, source_page_count, indexed_page_count, error_class}
- service.py: tags `["rag", "answer"]`, keys {boundary, answer_status, reason_code, run_id, provider_name, trace_id, top_score, citation_count, evidence_reason, error_class}
- pipeline.py: already routes through `safe_update_current_trace(..., context=langfuse_context)` with `_EXTRACTION_TRACE_ALLOWED_KEYS` — only its dead import needs replacing.

Verified CLI surface for README (do not invent commands):
- Ingest: `python -m src.pipeline ingest <folder> --db-path compliance.db` (typer command `ingest`, `--skip-existing/--no-skip-existing`)
- Retrieval index: `python -m src.retrieval build --db-path compliance.db` and `python -m src.retrieval status --db-path compliance.db`
- Extraction: `python -m src.extraction.cli extract --doc-id <id> --db-path compliance.db` and `python -m src.extraction.cli extract-all --db-path compliance.db [--provider gemini] [--visual-fallback]` (no src/extraction/__main__.py — module path is src.extraction.cli)
- Dashboard: `streamlit run src/app.py`
- Install: Python 3.11 venv, `pip install -e .` (dev extras: `pip install -e ".[dev]"`); pyproject name `pfizer-sdf-intelligence`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dead Langfuse v2 imports with the v3 client seam</name>
  <files>src/tracing.py, src/retrieval/retriever.py, src/retrieval/indexer.py, src/rag/service.py, src/extraction/pipeline.py</files>
  <action>
1. **src/tracing.py — make the lazy fallback return the live v3 client:**
   Rewrite `_get_langfuse_context()` (currently imports nonexistent `langfuse.decorators`, always returns None):
   ```python
   def _get_langfuse_context() -> Any | None:
       """Return the live Langfuse v3 client lazily; None when the SDK is unavailable."""
       try:
           client = get_client()
       except Exception:
           return None
       return client
   ```
   `get_client` is already imported at module top with a no-op fallback that raises RuntimeError when langfuse is absent — the try/except covers that. The caller (`safe_update_current_trace`) already guards with `hasattr(trace_context, "update_current_trace")`; the v3 client has that method, and a key-less v3 client is a disabled no-op, so offline behavior is unchanged. Do NOT change the `safe_update_current_trace` signature (tests inject `context=`).

   Also fix the module docstring (lines 7-14): it incorrectly labels `from langfuse.decorators import langfuse_context, observe` as v3. Correct it to state: v3 paths are `from langfuse import observe, get_client`, `get_client().update_current_trace(...)`, `get_client().auth_check()`; `langfuse.decorators` is the dead v2 path and must not be used anywhere.

2. **src/retrieval/retriever.py, src/retrieval/indexer.py, src/rag/service.py — same recipe each:**
   - Delete the `try: from langfuse.decorators import langfuse_context, observe ... except ImportError:` block including the local no-op `observe` def.
   - Add `from src.tracing import observe, safe_update_current_trace` (place with the other `src.` imports; `observe` from src.tracing already no-ops when langfuse is absent, so decorators keep working).
   - Keep BOTH module-level seam symbols (tests monkeypatch them by name):
     ```python
     # Injectable test seams: tests monkeypatch both symbols. langfuse_context=None
     # means "resolve the live v3 client lazily inside src.tracing".
     _LANGFUSE_AVAILABLE: bool = True
     langfuse_context: Any | None = None
     ```
   - Hoist each module's inline allowlist into a module-level frozenset constant (e.g. `_RETRIEVAL_TRACE_ALLOWED_KEYS`, `_INDEX_TRACE_ALLOWED_KEYS`, `_ANSWER_TRACE_ALLOWED_KEYS`) with the EXACT keys listed in interfaces above.
   - Rewrite each `_safe_update_trace_metadata(metadata)` to route through tracing:
     ```python
     def _safe_update_trace_metadata(metadata: dict[str, Any]) -> None:
         if not _LANGFUSE_AVAILABLE:
             return
         safe_update_current_trace(
             tags=["retrieval", "evidence"],          # per-module tags, unchanged
             metadata=metadata,
             allowed_metadata_keys=_RETRIEVAL_TRACE_ALLOWED_KEYS,
             context=langfuse_context,
         )
     ```
     Read `_LANGFUSE_AVAILABLE` / `langfuse_context` as module globals at call time (plain references, as shown) so monkeypatching works. Keep existing docstrings about no-op safety. Behavior matrix this must satisfy:
     - `_LANGFUSE_AVAILABLE=False, langfuse_context=None` (test 3) → no-op
     - `_LANGFUSE_AVAILABLE=True, langfuse_context=fake` (tests 1/4) → fake records filtered update with tags+metadata
     - `_LANGFUSE_AVAILABLE=True, langfuse_context=failing-fake` (test 2) → exception swallowed inside safe_update_current_trace, zero updates recorded
     - production defaults (`True`/`None`) → safe_update falls back to `_get_langfuse_context()` → live v3 client (or None offline)

3. **src/extraction/pipeline.py:** Replace the dead block at lines 17-20 (`try: from langfuse.decorators import langfuse_context ...`) with a plain module-level seam:
   ```python
   # Injectable test seam: tests monkeypatch pipeline.langfuse_context. None means
   # "resolve the live v3 client lazily inside src.tracing" (context=None fallback).
   langfuse_context: Any | None = None
   ```
   No other changes — it already imports `observe`/`safe_update_current_trace` from src.tracing and passes `context=langfuse_context`.

4. **Repo-wide sweep:** Grep src/ for `langfuse.decorators` — zero matches must remain (including the tracing.py docstring fixed in step 1).
  </action>
  <verify>
    <automated>venv/Scripts/python.exe -m pytest tests/test_tracing.py tests/test_retriever.py tests/test_retrieval_indexer.py tests/test_answer_service.py tests/test_extraction_pipeline.py -q</automated>
  </verify>
  <done>All five targeted test files pass; `grep -rn "langfuse.decorators" src/` returns nothing; `venv/Scripts/python.exe -c "from src.tracing import _get_langfuse_context; print(type(_get_langfuse_context()))"` prints a Langfuse client class (not None) since the SDK is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove root junk and write README.md</name>
  <files>README.md, "=2.72.0,", empty-dashboard.db, populated-dashboard.db, sdf_intel.db</files>
  <action>
1. **Junk removal:**
   - `git rm -- "=2.72.0,"` (tracked 0-byte pip-quoting accident at repo root; the `--` guards against option parsing).
   - Delete the three untracked 0-byte files at root: `empty-dashboard.db`, `populated-dashboard.db`, `sdf_intel.db` (all verified 0 bytes and gitignored — plain filesystem delete, no git command needed).
   - Do NOT touch `compliance.db` (27MB live data), `private/`, or `local_data/`.

2. **Write README.md at repo root** using ONLY the verified CLI commands from the interfaces block. Required sections:
   - **Title + one-paragraph purpose:** Pfizer SDF Intelligence System — AI-powered pharmaceutical supplier-document (SDF) compliance intelligence: ingestion → field extraction with grounded abstention → deterministic hybrid retrieval + evidence-gated RAG chat → Streamlit compliance dashboard → eval harness. Core value: a compliance officer uploads supplier PDFs, immediately sees expired/at-risk documents, asks questions across the corpus, and every answer is grounded in a cited source page — no hallucination.
   - **Architecture:** Docling/Granite-Docling ingestion to SQLite (compliance.db); Gemini 2.5 Flash extraction with verbatim-span grounding and opt-in visual fallback; deterministic SQLite FTS5 + lexical scoring retrieval (Tier 1); evidence-gated answer service (providers never called on weak evidence; citations derived only from retrieval hits); Streamlit 3-tab dashboard; run-scoped eval history persisted in compliance.db; Langfuse v3 tracing (optional, no-op without keys).
   - **Setup:** Python 3.11 venv; `pip install -e .` (or `pip install -e ".[dev]"` for pytest); `.env` with `GEMINI_API_KEY` (required for extraction/chat) and optional `LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` / `LANGFUSE_HOST` (tracing is a silent no-op without them). On Windows run tests with `venv\Scripts\python.exe -m pytest`.
   - **Demo flow (exact commands):**
     ```
     python -m src.pipeline ingest <pdf-folder> --db-path compliance.db
     python -m src.retrieval build --db-path compliance.db
     python -m src.retrieval status --db-path compliance.db
     python -m src.extraction.cli extract-all --db-path compliance.db
     python -m src.extraction.cli extract-all --db-path compliance.db --visual-fallback
     python -m src.extraction.cli extract --doc-id <id> --db-path compliance.db
     streamlit run src/app.py
     ```
   - **Retrieval tiers (explicit section):** Tier 1 (current) is deterministic SQLite FTS5 + lexical scoring — chosen for auditability, fully-offline operation, and zero infrastructure, which matters for compliance review. Tier 2 (designed, deferred) adds Qdrant + ColQwen2.5 visual retrieval behind the same evidence gate.
   - **Testing:** `venv\Scripts\python.exe -m pytest` — 303 tests, all offline (no API keys needed).
   - **Data hygiene:** confidential supplier PDFs and databases are gitignored (`*.db`, `private/`, `local_data/`) and never committed.
  </action>
  <verify>
    <automated>venv/Scripts/python.exe -c "import pathlib; r=pathlib.Path('README.md').read_text(encoding='utf-8'); assert 'python -m src.retrieval build' in r and 'python -m src.extraction.cli extract-all' in r and 'Tier 1' in r and 'GEMINI_API_KEY' in r; assert not pathlib.Path('=2.72.0,').exists(); assert not pathlib.Path('sdf_intel.db').exists(); assert pathlib.Path('compliance.db').exists(); print('ok')"</automated>
  </verify>
  <done>README.md exists with all required sections and verified commands; `git status` shows `=2.72.0,` staged for deletion; the three 0-byte db files are gone; compliance.db, private/, local_data/ untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression run</name>
  <files>(none — verification only)</files>
  <action>
Run the full offline test suite once (~3.5 min) to confirm the tracing seam changes introduced no regressions anywhere (CLI tests, eval runner tests, dashboard tests all exercise the modified modules indirectly). Per CLAUDE.md Windows rule: never /bin/bash, never ./venv. Note pytest.ini uses `-x` so it stops at first failure — if a failure appears, fix the seam per Task 1's behavior matrix and re-run.
  </action>
  <verify>
    <automated>venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <done>All 303 tests pass with no failures or errors.</done>
</task>

</tasks>

<verification>
- `grep -rn "langfuse.decorators" src/` → no matches
- Full suite: 303 passed (offline, no Langfuse/Gemini keys)
- README.md present at root with purpose, architecture, setup, demo commands, retrieval tiers, data hygiene
- Root contains no `=2.72.0,`, no 0-byte db files; compliance.db intact at ~27MB
</verification>

<success_criteria>
- Tracing is live-capable: with Langfuse keys configured, retriever/indexer/service/pipeline metadata reaches the v3 client; without keys, everything no-ops exactly as before
- All existing test seams (monkeypatched `_LANGFUSE_AVAILABLE` / `langfuse_context` module symbols) work unchanged
- New contributor can clone, read README.md, and run the full demo flow from documented commands
- Repo root is clean of pip/db junk
</success_criteria>

<output>
After completion, create `.planning/quick/260610-2y6-fix-dead-langfuse-v2-imports-add-readme-/260610-2y6-SUMMARY.md`
</output>
